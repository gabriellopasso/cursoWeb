const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b +  c + d

const subtração = b - a

const multiplicação = a * b

const divisão = d/a

const modulo = c % 2 //resto

console.log(soma, subtração, multiplicação, divisão, modulo)